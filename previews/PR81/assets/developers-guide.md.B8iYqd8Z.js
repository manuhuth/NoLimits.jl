import{_ as a,o as s,c as t,ao as i}from"./chunks/framework.BD_nsxNn.js";const u=JSON.parse('{"title":"Developers Guide","description":"","frontmatter":{},"headers":[],"relativePath":"developers-guide.md","filePath":"developers-guide.md","lastUpdated":null}'),n={name:"developers-guide.md"};function o(l,e,r,d,c,p){return s(),t("div",null,[...e[0]||(e[0]=[i(`<h1 id="Developers-Guide" tabindex="-1">Developers Guide <a class="header-anchor" href="#Developers-Guide" aria-label="Permalink to &quot;Developers Guide {#Developers-Guide}&quot;">​</a></h1><p>This page is for contributors and maintainers working on NoLimits.jl itself. For using the package, see <a href="./quickstart">Quickstart</a> and the <a href="./tutorials/mixed-effects-multiple-methods">Tutorials</a>.</p><h2 id="Scope" tabindex="-1">Scope <a class="header-anchor" href="#Scope" aria-label="Permalink to &quot;Scope {#Scope}&quot;">​</a></h2><p>NoLimits.jl is a framework for nonlinear mixed-effects modeling of longitudinal data. The codebase is organized around three concerns: a macro-based modeling language (<code>@Model</code> and its block macros), data binding and validation (<code>DataModel</code>), and a family of estimation methods that all share the <code>fit_model</code> interface. Contributions should preserve this separation and the conventions described below.</p><h2 id="Codebase-Layout" tabindex="-1">Codebase Layout <a class="header-anchor" href="#Codebase-Layout" aria-label="Permalink to &quot;Codebase Layout {#Codebase-Layout}&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>src/</span></span>
<span class="line"><span>├── NoLimits.jl          # Module definition and includes</span></span>
<span class="line"><span>├── Constants.jl         # Global constants (scales, epsilon)</span></span>
<span class="line"><span>├── model/               # Modeling language: @Model and its blocks</span></span>
<span class="line"><span>│   ├── Model.jl                 # @Model macro and the Model struct/bundles</span></span>
<span class="line"><span>│   ├── Parameters.jl            # Fixed-effect parameter blocks and scales</span></span>
<span class="line"><span>│   ├── FixedEffects.jl          # @fixedEffects</span></span>
<span class="line"><span>│   ├── RandomEffects.jl         # @randomEffects and RE distributions</span></span>
<span class="line"><span>│   ├── Covariates.jl            # @covariates (constant / varying / dynamic)</span></span>
<span class="line"><span>│   ├── Helpers.jl               # @helpers</span></span>
<span class="line"><span>│   ├── PreDE.jl                 # @preDifferentialEquation</span></span>
<span class="line"><span>│   ├── DifferentialEquation.jl  # @DifferentialEquation</span></span>
<span class="line"><span>│   ├── InitialDE.jl             # @initialDE</span></span>
<span class="line"><span>│   └── Formulas.jl              # @formulas (deterministic nodes + observations)</span></span>
<span class="line"><span>├── data_model/          # DataModel construction, validation, batching, events</span></span>
<span class="line"><span>├── data_simulation/     # simulate_data / simulate_data_model</span></span>
<span class="line"><span>├── estimation/          # MLE, MAP, MCMC, VI, Laplace, FOCEI, GHQuadrature,</span></span>
<span class="line"><span>│                        #   MCEM, SAEM, Pooled, Multistart, cross-validation, UQ</span></span>
<span class="line"><span>├── plotting/            # Fits, VPC, residual and random-effect diagnostics</span></span>
<span class="line"><span>├── distributions/       # Custom outcome (HMM/Markov) and RE (flow) distributions</span></span>
<span class="line"><span>├── soft_trees/          # Soft decision trees</span></span>
<span class="line"><span>└── utils/               # Transforms, splines, general utilities</span></span></code></pre></div><p>The repository also contains the test suite under <code>test/</code>, this documentation under <code>docs/</code>, and standard project files. Local-only scratch directories (e.g. <code>benchmarks/</code>, <code>examples/</code>, <code>tmp/</code>) and editor/build artifacts are listed in <code>.gitignore</code> and must not be committed.</p><h2 id="Local-Development-Setup" tabindex="-1">Local Development Setup <a class="header-anchor" href="#Local-Development-Setup" aria-label="Permalink to &quot;Local Development Setup {#Local-Development-Setup}&quot;">​</a></h2><p>Clone the repository and instantiate the project environment:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> clone</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://github.com/manuhuth/NoLimits.jl</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> NoLimits.jl</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;using Pkg; Pkg.instantiate()&#39;</span></span></code></pre></div><p><code>Manifest.toml</code> is intentionally not tracked, so <code>Pkg.instantiate()</code> resolves dependencies against the <code>[compat]</code> bounds in <code>Project.toml</code>.</p><h2 id="Coding-Conventions" tabindex="-1">Coding Conventions <a class="header-anchor" href="#Coding-Conventions" aria-label="Permalink to &quot;Coding Conventions {#Coding-Conventions}&quot;">​</a></h2><p>A few conventions are load-bearing and enforced throughout the codebase:</p><ul><li><p><strong>Differentiability.</strong> All model and likelihood code must be differentiable. ForwardDiff, the default backend, propagates <code>Dual</code> numbers and handles in-place mutation fine; what it needs is <em>type-generic</em> code that lets a <code>Dual</code> flow through, so allocate temporaries from the input element type (<code>similar(x)</code>, <code>zeros(eltype(x), n)</code>) rather than a hard-coded <code>Float64</code>. Non-mutating implementations are preferred where code may also run through a reverse-mode backend that cannot differentiate array mutation. See <a href="./model-building/helpers">Helpers</a>.</p></li><li><p><strong>Accessor functions.</strong> Struct fields are reached through <code>get_*</code> accessor functions rather than direct field access. This applies internally and, above all, in the public API: every user-facing type ships <code>get_*</code> accessors, so users never have to reach into a struct with dot syntax. Field layout is an implementation detail and may change between versions.</p></li><li><p><strong><code>ComponentArray</code> construction.</strong> Reuse existing axes with <code>ComponentArray(values, getaxes(existing))</code> rather than reconstructing axes.</p></li><li><p><strong>Formatting (JuliaFormatter).</strong> Source, tests, and docs follow the SciML style, pinned in <code>.JuliaFormatter.toml</code> (<code>style = &quot;sciml&quot;</code>). Run <a href="https://github.com/domluna/JuliaFormatter.jl" target="_blank" rel="noreferrer">JuliaFormatter.jl</a> v1 before committing (<code>julia -e &#39;using JuliaFormatter; format([&quot;src&quot;, &quot;test&quot;, &quot;docs&quot;])&#39;</code>), since the <code>Format</code> CI job fails on any diff. Use v1 specifically; v2 formats differently and produces spurious diffs.</p></li><li><p><strong>Static quality (Aqua.jl).</strong> <code>test/aqua_tests.jl</code> runs <code>Aqua.test_all(NoLimits)</code> with no ignore lists, so contributions must keep method ambiguities at zero, avoid type piracy, and give every dependency a <code>[compat]</code> entry. It runs as part of the test suite.</p></li></ul><p>See the source of an existing estimation method (for example <code>src/estimation/laplace.jl</code>) for the expected style when adding a new method.</p><h2 id="Testing-Strategy" tabindex="-1">Testing Strategy <a class="header-anchor" href="#Testing-Strategy" aria-label="Permalink to &quot;Testing Strategy {#Testing-Strategy}&quot;">​</a></h2><p>The suite lives in <code>test/</code>, with files grouped by component (<code>estimation_*_tests.jl</code>, <code>data_model_*_tests.jl</code>, <code>ad_*.jl</code>, and so on). Run the whole suite with:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;using Pkg; Pkg.test()&#39;</span></span></code></pre></div><p><code>test/runtests.jl</code> runs the suite as a sequence of <code>julia</code> <strong>subprocess batches</strong> rather than a single process. Each distinct <code>@Model</code> emits type-specialized native code that Julia does not free within a process, so running all files in one process accumulates compiled code and exhausts memory; exiting between batches caps per-process memory. The batch count can be overridden with the <code>NL_BATCHES</code> environment variable.</p><p>To iterate on a single area, run an individual test file directly:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test/estimation_laplace_tests.jl</span></span></code></pre></div><p>Automatic-differentiation correctness is covered by the <code>ad_*.jl</code> files, which check gradients and Hessians of the likelihood and random-effects terms. When adding a feature, add or extend the corresponding test file and keep it wired into the <code>TEST_FILES</code> list in <code>test/runtests.jl</code>.</p><h2 id="Documentation-Workflow" tabindex="-1">Documentation Workflow <a class="header-anchor" href="#Documentation-Workflow" aria-label="Permalink to &quot;Documentation Workflow {#Documentation-Workflow}&quot;">​</a></h2><p>The documentation is built with <a href="https://documenter.juliadocs.org" target="_blank" rel="noreferrer">Documenter.jl</a> and the <a href="https://luxdl.github.io/DocumenterVitepress.jl" target="_blank" rel="noreferrer">DocumenterVitepress.jl</a> theme, with a bibliography managed by <a href="https://juliadocs.org/DocumenterCitations.jl" target="_blank" rel="noreferrer">DocumenterCitations.jl</a>. It uses its own environment in <code>docs/Project.toml</code>.</p><p>Build and preview locally:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># one-time: resolve the docs environment and dev-install the package into it</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=docs</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;using Pkg; Pkg.develop(path=&quot;.&quot;); Pkg.instantiate()&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># build the site (CairoMakie renders headlessly, no display setup needed)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=docs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docs/make.jl</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># preview the built site (a local build writes the rendered site to docs/build/1)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">julia</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --project=docs</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -e</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;using LiveServer; LiveServer.serve(dir=&quot;docs/build/1&quot;)&#39;</span></span></code></pre></div><p>Tutorials are <strong>precomputed offline</strong> rather than executed during the build: the scripts in <code>docs/scripts/</code> (notably <code>precompute_tutorials.jl</code>) run the expensive fits once and inject their text output and figures into the tutorial pages via <code>&lt;!-- injected:... --&gt;</code> markers and the <code>docs/src/tutorials/figures/</code> directory. This keeps the Documenter build fast and deterministic. When changing a tutorial&#39;s code, re-run the relevant precompute script so the injected outputs stay in sync. New entries in the bibliography go in <code>docs/src/references.bib</code> and are cited inline with the <code>[key](@cite)</code> syntax.</p><h2 id="Release-Process" tabindex="-1">Release Process <a class="header-anchor" href="#Release-Process" aria-label="Permalink to &quot;Release Process {#Release-Process}&quot;">​</a></h2><p>Releases follow the standard Julia workflow: 2. Bump the <code>version</code> field in <code>Project.toml</code> (semantic versioning) and update any <code>[compat]</code> bounds as needed.</p><ol start="3"><li><p>Open a pull request against <code>main</code> and wait for continuous integration to pass. <code>main</code> is a protected branch: it accepts no direct pushes, and a pull request can be merged only after the required CI checks (test suite, formatting, and documentation build) are green. This is enforced for maintainers too.</p></li><li><p>Register the new version in the Julia General registry (e.g. via the Registrator bot). <code>TagBot</code> then creates the matching Git tag and GitHub release automatically, and the documentation for the tagged version is deployed to GitHub Pages.</p></li></ol><h2 id="How-to-Contribute" tabindex="-1">How to Contribute <a class="header-anchor" href="#How-to-Contribute" aria-label="Permalink to &quot;How to Contribute {#How-to-Contribute}&quot;">​</a></h2><p>See <a href="./how-to-contribute">How to Contribute</a> for the issue and pull-request workflow.</p>`,32)])])}const g=a(n,[["render",o]]);export{u as __pageData,g as default};
