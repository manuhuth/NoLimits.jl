export REAL_SCALES
export PSD_SCALES
export DIAGONAL_SCALES
export EPSILON

const REAL_SCALES = (:identity, :log)
const PSD_SCALES = (:cholesky, :expm)
const DIAGONAL_SCALES = (:log,)
const EPSILON = 0.0
